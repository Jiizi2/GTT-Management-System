# Host port exposed by the `web` container on the VPS.
# Keep 8080 for now unless another service on the VPS already uses it.
WEB_PORT=8080

# Internal PostgreSQL credentials for this application's own database container.
POSTGRES_USER=gtt_app
POSTGRES_PASSWORD=replace-with-a-strong-db-password
POSTGRES_DB=gtt_ops
